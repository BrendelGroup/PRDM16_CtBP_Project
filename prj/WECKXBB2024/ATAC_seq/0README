# In this directory (ATAC_seq), the "0Record" file will execute all the
#  bash scripts to analysize the ATAC-seq data, starting from
#  downloading the raw reads up to generating the bigWig files:

bash ./0Record 

# See comments in 0Record for more details on the bash scripts.
